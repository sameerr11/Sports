const express = require('express');
const router = express.Router();

// Import routes
// Example: const authRoutes = require('./authRoutes');

// Use routes
// Example: router.use('/auth', authRoutes);

module.exports = router; 